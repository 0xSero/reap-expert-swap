# Configs

This directory is reserved for public-safe configuration templates and research batch settings.

Private host- or machine-specific runtime configs should not be committed to the public repo.
