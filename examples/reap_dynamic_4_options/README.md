# Example: reap_dynamic_4_options

The private research repo contains a larger option-comparison subtree.
This public staging tree keeps the slot for a future public-safe example export without shipping private residue or local virtual environments.
